---
import type { PostMeta } from '../../lib/notion';

interface Props {
  /** 上一篇（更新的一篇，按发布日期倒序中的前一条），无则 null */
  prev: PostMeta | null;
  /** 下一篇（更旧的一篇），无则 null */
  next: PostMeta | null;
}

const { prev, next } = Astro.props;
---

{
  (prev || next) && (
    <nav class="post-nav" aria-label="文章导航">
      {prev ? (
        <a class="post-nav-item prev" href={`/posts/${prev.slug}`}>
          <span class="post-nav-label">上一篇</span>
          <span class="post-nav-title">{prev.title}</span>
        </a>
      ) : (
        <span class="post-nav-item prev disabled" aria-hidden="true" />
      )}
      {next ? (
        <a class="post-nav-item next" href={`/posts/${next.slug}`}>
          <span class="post-nav-label">下一篇</span>
          <span class="post-nav-title">{next.title}</span>
        </a>
      ) : (
        <span class="post-nav-item next disabled" aria-hidden="true" />
      )}
    </nav>
  )
}

<style>
  /* 暖面板内的上下篇导航（阶段 5 暖调） */
  .post-nav {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-top: 2.6rem;
    padding-top: 1.6rem;
    border-top: 1px solid var(--hairline);
  }
  .post-nav-item {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    padding: 0.85rem 1rem;
    border: 1px solid var(--hairline);
    border-radius: 3px;
    text-decoration: none;
    background: rgba(216, 168, 102, 0.04);
    min-width: 0;
    transition: border-color 200ms ease-out, box-shadow 200ms ease-out,
      transform 200ms ease-out, color 200ms ease-out;
  }
  .post-nav-item.next {
    text-align: right;
  }
  .post-nav-item.disabled {
    border: none;
    background: transparent;
  }
  .post-nav-label {
    font-family: var(--font-serif);
    font-size: 0.78rem;
    color: var(--mist);
    letter-spacing: 0.1em;
  }
  .post-nav-title {
    font-size: 0.92rem;
    color: var(--warm-text);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  html.light .post-nav-title {
    color: #3a342c;
  }
  .post-nav-item:hover {
    border-color: var(--amber);
    box-shadow: 0 0 16px rgba(216, 168, 102, 0.15);
    transform: translateY(-2px);
  }
  .post-nav-item:hover .post-nav-title {
    color: var(--amber-soft);
  }
  html.light .post-nav-item:hover .post-nav-title {
    color: #8a5a24;
  }
  @media (max-width: 600px) {
    .post-nav {
      grid-template-columns: 1fr;
    }
    .post-nav-item.next {
      text-align: left;
    }
  }
</style>
